/*

import { createSelector } from 'reselect'

const dataSelector = state => state.comment.list

export const getData = createSelector(
    dataSelector,
    data => {
        if (data.hasOwnProperty('results')){
            return {
                results: data.results
            }
        } else {
            return {}
        }
    }
)
*/
