
/*

import * as types from "./types";

export const requestComment = (count) => ({
    type: types.REQUEST_COMMENT,
    payload: {
        count
    }
})

export const loadComment = loading => ({
    type: types.LOAD_COMMENT,
    payload: {
        loading
    }
})

export const requestCommentSuccess = data => ({
    type: types.REQUEST_COMMENT_SUCCESS,
    payload: {
        data
    }
})

export const requestCommentFailed = error => ({
    type: types.REQUEST_COMMENT_FAILED,
    payload: {
        error
    }
})

export const submitComment = (content, userID, currentNoComment) => ({
    type: types.SUBMIT_COMMENT_SAGA,
    payload: {
        content,
        userID
    }
})

*/